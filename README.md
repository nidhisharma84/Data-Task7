This task focuses on advanced SQL analytics using window functions.

Dataset used: Global Superstore Dataset imported from CSV into PostgreSQL.

Performed sales aggregation using GROUP BY.

Ranked customers by region using ROW_NUMBER(), RANK(), and DENSE_RANK().

Compared ranking functions to understand tie handling.

Calculated running total of sales using SUM() OVER(ORDER BY date).

Computed Month-over-Month growth using LAG().

Identified Top 3 products per category using CTE and DENSE_RANK().

Exported results as CSV files and documented insights.

This task demonstrates real-world SQL reporting and analytical skills
